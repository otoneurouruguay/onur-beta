import { ArrowDown, ArrowUp, ChevronLeft, Copy, Plus, Save, Trash2 } from 'lucide-react'
import { useEffect, useRef, useState, type FormEvent } from 'react'
import { Link, useNavigate, useParams } from 'react-router-dom'
import { PageHeader } from '../components/PageHeader'
import { defaultExerciseConfig, type ExerciseConfig } from '../features/exercise/types'
import { usePatient } from '../features/patients/hooks'
import { useCreateSessionAssignment, useSessionAssignments, useTreatmentCycles, useUpdateSessionAssignment } from '../features/sessions/hooks'
import { canManageSessionAssignment } from '../features/sessions/repository'
import { appendExerciseTemplate, DEFAULT_SESSION_TITLE } from '../features/sessions/builder'
import { SessionExerciseEditor } from '../features/sessions/SessionExerciseEditor'
import { SessionSequenceWarning } from '../features/sessions/SessionSequenceWarning'
import { validateSession, type SessionFormValues } from '../features/sessions/schema'
import { useExerciseTemplates } from '../features/templates/hooks'
import { groupExerciseTemplates } from '../features/templates/grouping'

export function SessionBuilderPage(){
  const{patientId='',assignmentId}=useParams();const navigate=useNavigate();const{data:patient}=usePatient(patientId);const{data:cycles=[]}=useTreatmentCycles(patientId);const{data:assignments=[],isPending:assignmentsPending}=useSessionAssignments(patientId);const create=useCreateSessionAssignment(patientId);const update=useUpdateSessionAssignment(patientId);const[selected,setSelected]=useState(0);const[errors,setErrors]=useState<Record<string,string>>({})
  const assignment=assignmentId?assignments.find(item=>item.id===assignmentId):undefined
  const isEditing=Boolean(assignmentId)
  const initializedAssignmentId=useRef('')
  const { data: templates = [] } = useExerciseTemplates()
  const templateGroups = groupExerciseTemplates(templates)
  const[values,setValues]=useState<SessionFormValues>({title:DEFAULT_SESSION_TITLE,instructions:'Realizar según las indicaciones brindadas por el profesional.',mode:'home',treatmentCycleId:'',availableFrom:new Date().toISOString().slice(0,10),availableUntil:'',exercises:[{...defaultExerciseConfig}]})
  useEffect(()=>{
    if(!assignmentId||!assignment||initializedAssignmentId.current===assignmentId)return
    initializedAssignmentId.current=assignmentId
    setValues({title:assignment.title,instructions:assignment.instructions,mode:assignment.mode,treatmentCycleId:assignment.treatmentCycleId,availableFrom:assignment.availableFrom.slice(0,10),availableUntil:assignment.availableUntil.slice(0,10),exercises:assignment.exercises.map(exercise=>({...exercise}))})
    setSelected(0)
  },[assignment,assignmentId])
  useEffect(()=>{if(isEditing)return;const active=cycles.find(c=>c.status==='active');if(active&&!values.treatmentCycleId)setValues(v=>({...v,treatmentCycleId:active.id}))},[cycles,isEditing,values.treatmentCycleId])
  const clearExerciseError=()=>setErrors(current=>{if(!current.exercises)return current;const next={...current};delete next.exercises;return next})
  const updateExercise=(config:ExerciseConfig)=>{setValues(v=>({...v,exercises:v.exercises.map((e,i)=>i===selected?config:e)}));clearExerciseError()};const move=(direction:-1|1)=>{const target=selected+direction;if(target<0||target>=values.exercises.length)return;setValues(v=>{const next=[...v.exercises];[next[selected],next[target]]=[next[target],next[selected]];return{...v,exercises:next}});setSelected(target);clearExerciseError()}
  const submit=async(event:FormEvent)=>{event.preventDefault();const found=validateSession(values);setErrors(found);if(Object.keys(found).length)return;try{if(isEditing&&assignment){await update.mutateAsync({assignment,values});navigate(`/app/pacientes/${patientId}`,{state:{notice:'Sesión actualizada correctamente.'}})}else{await create.mutateAsync(values);navigate(`/app/pacientes/${patientId}`,{state:{notice:'Sesión creada y asignada correctamente.'}})}}catch(caught){setErrors({form:caught instanceof Error?caught.message:isEditing?'No fue posible actualizar la sesión.':'No fue posible crear la sesión.'})}}
  const input='mt-2 h-11 w-full rounded-2xl border border-[#E9E7E7] bg-white px-3 text-sm'
  if(isEditing&&assignmentsPending)return <p className="text-sm text-[#747474]">Cargando sesión…</p>
  if(isEditing&&!assignment)return <div className="space-y-4"><p className="text-sm text-[#747474]">Sesión no encontrada.</p><Link to={`/app/pacientes/${patientId}`} className="text-xs font-black text-[#E49A02]">Volver al perfil</Link></div>
  if(assignment&&!canManageSessionAssignment(assignment))return <div className="space-y-4"><p className="rounded-2xl bg-[#FFF7E8] p-5 text-sm font-bold text-[#8A5B00]">Esta sesión ya tiene actividad registrada. Para conservar el historial clínico no puede modificarse.</p><Link to={`/app/pacientes/${patientId}`} className="text-xs font-black text-[#E49A02]">Volver al perfil</Link></div>
  return <div className="space-y-7"><Link to={`/app/pacientes/${patientId}`} className="inline-flex items-center gap-2 text-xs font-black text-[#E49A02]"><ChevronLeft size={16}/> Volver al perfil</Link><PageHeader eyebrow="Planificación" title={isEditing?'Editar sesión':'Crear y asignar sesión'} description={patient?`Configuración para ${patient.fullName}. Cada ejercicio mantiene fondo y objeto independientes.`:'Prepará una sesión.'}/>
    {cycles.length===0?<section className="rounded-2xl border border-[#E8CE99] bg-[#FFF7E8] p-6"><h2 className="font-black text-[#8A5B00]">Primero necesitás un ciclo activo</h2><p className="mt-2 text-sm text-[#8A5B00]">Las sesiones quedan asociadas al ciclo para conservar el historial y preparar el informe final.</p><Link to={`/app/pacientes/${patientId}/ciclos/nuevo`} className="mt-4 inline-flex rounded-2xl bg-[#8A5B00] px-4 py-3 text-sm font-black text-white">Iniciar ciclo</Link></section>:<form onSubmit={submit} className="space-y-7"><section className="grid gap-5 rounded-2xl border border-[#E9E7E7] bg-white p-6 md:grid-cols-2 xl:grid-cols-4"><label className="text-xs font-black text-[#2F2F2F] xl:col-span-2">Título<input className={input} value={values.title} onChange={e=>setValues(v=>({...v,title:e.target.value}))}/>{errors.title&&<small className="text-[#a94952]">{errors.title}</small>}</label><label className="text-xs font-black text-[#2F2F2F]">Modalidad<select className={input} value={values.mode} onChange={e=>{setValues(v=>({...v,mode:e.target.value as SessionFormValues['mode']}));clearExerciseError()}}><option value="home">Domiciliaria</option><option value="in_person">Presencial</option></select></label><label className="text-xs font-black text-[#2F2F2F]">Ciclo<select className={input} value={values.treatmentCycleId} onChange={e=>setValues(v=>({...v,treatmentCycleId:e.target.value}))}>{cycles.filter(c=>c.status!=='completed').map(c=><option key={c.id} value={c.id}>{c.label} · {c.status}</option>)}</select></label><label className="text-xs font-black text-[#2F2F2F]">Disponible desde<input type="date" className={input} value={values.availableFrom} onChange={e=>setValues(v=>({...v,availableFrom:e.target.value}))}/></label><label className="text-xs font-black text-[#2F2F2F]">Disponible hasta (opcional)<input type="date" className={input} value={values.availableUntil} onChange={e=>setValues(v=>({...v,availableUntil:e.target.value}))}/></label><label className="text-xs font-black text-[#2F2F2F] md:col-span-2">Indicaciones para el paciente<textarea className="mt-2 min-h-20 w-full rounded-2xl border border-[#E9E7E7] p-3 text-sm" value={values.instructions} onChange={e=>setValues(v=>({...v,instructions:e.target.value}))}/></label></section>
      <section><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-xl font-black text-[#171717]">Ejercicios</h2><p className="mt-1 text-xs text-[#747474]">Se reproducen en este orden. VR Box admite transiciones con el mismo celular; una sesión Quest debe ser íntegramente Quest. Una sesión contextual 360° puede reunir varios escenarios compatibles y activa la modalidad presencial.</p></div><div className="flex gap-2"><select defaultValue="" onChange={event=>{const template=templates.find(item=>item.id===event.target.value);if(template){setValues(current=>{const result=appendExerciseTemplate(current,template.config);setSelected(result.selectedIndex);return result.values})}event.target.value='';clearExerciseError()}} className="rounded-2xl border border-[#E9E7E7] bg-white px-3 text-xs font-black text-[#2F2F2F]"><option value="">Agregar desde biblioteca…</option>{templateGroups.map(group=><optgroup key={group.id} label={group.label}>{group.templates.map(template=><option key={template.id} value={template.id}>{template.config.clinicalProtocol==='pppd'?`Nivel ${template.config.progressionLevel} · `:''}{template.name.replace(/^PPPD · (Habituación visual|Optocinético|Funcional) · /,'')}</option>)}</optgroup>)}</select><button type="button" onClick={()=>setValues(current=>{const exercises=[...current.exercises,{...defaultExerciseConfig,name:`Ejercicio ${current.exercises.length+1}`}];setSelected(exercises.length-1);return{...current,exercises}})} className="inline-flex items-center gap-2 rounded-2xl border border-[#E9E7E7] bg-white px-4 py-3 text-xs font-black text-[#2F2F2F]"><Plus size={16}/> Nuevo</button></div></div><SessionSequenceWarning exercises={values.exercises} onReorder={(exercises)=>{setValues(v=>({...v,exercises}));setSelected(0)}}/><div className="mb-5 flex gap-3 overflow-x-auto pb-2">{values.exercises.map((exercise,index)=><button key={`${exercise.name}-${index}`} type="button" onClick={()=>setSelected(index)} className={`min-w-48 rounded-2xl border p-4 text-left ${selected===index?'border-[#E49A02] bg-[#FFF7E8]':'border-[#E9E7E7] bg-white'}`}><span className="text-[10px] font-black uppercase text-[#E49A02]">Ejercicio {index+1}</span><p className="mt-2 truncate text-sm font-black text-[#2F2F2F]">{exercise.name}</p><p className="mt-1 text-xs text-[#747474]">{exercise.doseMode==='time'?`${exercise.durationSeconds}s`:`${exercise.targetRepetitions} rep.`} × {exercise.rounds}{exercise.displayMode==='vr_box'?' · VR Box':exercise.displayMode==='quest_browser'?' · Quest':''}</p></button>)}</div><div className="mb-4 flex gap-2"><button type="button" onClick={()=>move(-1)} className="rounded-xl border border-[#E9E7E7] p-2" aria-label="Mover antes"><ArrowUp size={16}/></button><button type="button" onClick={()=>move(1)} className="rounded-xl border border-[#E9E7E7] p-2" aria-label="Mover después"><ArrowDown size={16}/></button><button type="button" onClick={()=>setValues(v=>({...v,exercises:[...v.exercises,{...v.exercises[selected],name:`${v.exercises[selected].name} (copia)`}]}))} className="rounded-xl border border-[#E9E7E7] p-2" aria-label="Duplicar"><Copy size={16}/></button><button type="button" disabled={values.exercises.length===1} onClick={()=>{setValues(v=>({...v,exercises:v.exercises.filter((_,i)=>i!==selected)}));setSelected(Math.max(0,selected-1))}} className="rounded-xl border border-[#eccfd2] p-2 text-[#a94952] disabled:opacity-30" aria-label="Eliminar"><Trash2 size={16}/></button></div>{errors.exercises&&<p className="mb-4 text-sm font-bold text-[#a94952]">{errors.exercises}</p>}<SessionExerciseEditor config={values.exercises[selected]} isFirst={selected===0} setting={values.mode} onChange={updateExercise}/></section>
      {errors.form&&<p role="alert" className="rounded-2xl bg-[#fceced] p-4 text-sm font-bold text-[#a94952]">{errors.form}</p>}<div className="sticky bottom-4 z-10 flex justify-end"><button disabled={create.isPending||update.isPending} className="inline-flex items-center gap-2 rounded-2xl bg-[#E49A02] px-6 py-4 text-sm font-black text-white shadow-xl"><Save size={17}/>{isEditing?(update.isPending?'Guardando…':'Guardar cambios'):(create.isPending?'Asignando…':'Guardar y asignar sesión')}</button></div></form>}</div>
}
